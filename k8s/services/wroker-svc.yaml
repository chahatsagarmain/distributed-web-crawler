apiVersion: v1
kind: Service
metadata:
  name: wroker-svc
  namespace: crawler
spec:
  type: ClusterIP       
  selector:
    app: worker
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 8081