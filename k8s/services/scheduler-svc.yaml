apiVersion: v1
kind: Service
metadata:
  name: scheduler-svc
  namespace: crawler
spec:
  type: ClusterIP       
  selector:
    app: scheduler
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 8080  